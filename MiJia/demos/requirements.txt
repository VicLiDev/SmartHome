# 米家接入 Demo — Python 依赖

# 核心依赖（所有 demo 都需要）
pycryptodome>=3.19.0

# Demo 3: python-miio 库
python-miio>=0.5.0

# Demo 4/8/9/10/11: HTTP 请求
requests>=2.31.0
# micloud>=0.6.0  # 可选，独立云端库

# Demo 5/7: MQTT
paho-mqtt>=1.6.0

# Demo 6: BLE 监听
bleak>=0.21.0

# Demo 8: Home Assistant API（仅需 requests，此为可选增强）
# homeassistant>=2024.1  # 可选，直接导入 HA 实体定义

# Demo 9: Node-RED API（仅需 requests，无需额外依赖）

# Demo 10: 米家 App 回调（仅 stdlib，零依赖）

# Demo 11: 官方 IoT 平台（仅需 requests + stdlib）

# 可选: 定时调度
# schedule>=1.2.0
# croniter>=1.3.0

# 可选: 配置管理
# pyyaml>=6.0
